extend type Query {
	product(id: ID!): Product
	products(pageSize: Int, after: String): ProductPage!
}

type ProductPage {
	nodes: [Product!]!
	pageInfo: PageInfo!
}

type Product {
	id: ID!
	name: String!
	price: Price!
	description: String
}

type PageInfo {
	hasNextPage: Boolean!
	nextCursor: String
}
